DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    
	MOV AX,1470H
	AND AX, AX
	OR AX, AX
	NOT AX 
	TEST AX,0F0F0H
	MOV CL,4
	MOV AL, 0F0H
	SAR AL,1
	SAR AL, CL
	MOV CL,2
	ROR AL, CL
	MOV CL,4
	RCL AL, 4
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
